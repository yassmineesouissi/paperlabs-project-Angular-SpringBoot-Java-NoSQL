version: '2'
services:
  paperlabs-mongodb:
    image: mongo:4.0.12
    ports:
      - '27017:27017'
    command: mongos --configdb paperlabs-mongodb-config
  paperlabs-mongodb-node:
    build:
      context: .
      dockerfile: mongodb/MongoDB.Dockerfile
    command: mongod --replSet rs1 --noprealloc --smallfiles
  paperlabs-mongodb-config:
    image: mongo:4.0.12
    container_name: paperlabs-mongodb-config
    command: mongod --noprealloc --smallfiles --configsvr --dbpath /data/db
